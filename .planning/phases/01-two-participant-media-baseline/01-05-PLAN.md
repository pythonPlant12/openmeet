---
phase: 01-two-participant-media-baseline
plan: 5
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - openmeet-client/e2e/multi-participant-media.spec.ts
autonomous: false
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-04
  - MEDIA-05
  - MEDIA-06
  - VER-01
  - VER-02
must_haves:
  truths:
    - "D-09: Two browser participants on https://openmeets.eu/room/1/ both receive remote audio/video and remain connected through the observed failure window."
    - "D-10: Automated or scripted coverage proves the corrected two-participant path at the most practical level available."
    - "D-11/D-12: Any third-participant observation is recorded only as non-blocking Phase 2 signal, and the summary includes UI evidence plus client/server log markers."
  artifacts:
    - path: openmeet-client/e2e/multi-participant-media.spec.ts
      provides: "Final two-participant regression smoke with audio/video assertions"
      contains: "remote audio"
    - path: .planning/phases/01-two-participant-media-baseline/01-05-SUMMARY.md
      provides: "Execution evidence for all Phase 1 decisions and requirements"
      contains: "D-12"
  key_links:
    - from: openmeet-client/e2e/multi-participant-media.spec.ts
      to: openmeet-server/src/sfu/room.rs
      via: "browser remote media assertions backed by server forwarding logs"
      pattern: "participant-video|MediaStream"
---

<objective>
Perform final Phase 1 verification across the corrected two-participant browser, client, and server path, per D-09 through D-12.

Purpose: prove the baseline outcome, not merely that individual fixes compile.
Output: passing automated checks where feasible, real/browser-equivalent two-participant evidence, VPS smoke evidence, and a blocking human verification checkpoint for final UI/media confirmation.
</objective>

<execution_context>
@/Users/nikitalutsai/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nikitalutsai/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
@.planning/phases/01-two-participant-media-baseline/01-RESEARCH.md
@.planning/phases/01-two-participant-media-baseline/01-PATTERNS.md
@.planning/phases/01-two-participant-media-baseline/01-UI-SPEC.md
@.planning/phases/01-two-participant-media-baseline/01-01-SUMMARY.md
@.planning/phases/01-two-participant-media-baseline/01-02-SUMMARY.md
@.planning/phases/01-two-participant-media-baseline/01-03-SUMMARY.md
@.planning/phases/01-two-participant-media-baseline/01-04-SUMMARY.md
@openmeet-client/e2e/multi-participant-media.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden final two-participant smoke assertions</name>
  <files>openmeet-client/e2e/multi-participant-media.spec.ts</files>
  <read_first>
    @.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
    @.planning/phases/01-two-participant-media-baseline/01-PATTERNS.md
    @.planning/phases/01-two-participant-media-baseline/01-01-SUMMARY.md
    @openmeet-client/e2e/multi-participant-media.spec.ts
    @openmeet-client/src/components/meeting-page/ParticipantTile.vue
  </read_first>
  <behavior>
    - Both participants have local media and one remote participant tile.
    - Each remote tile has a MediaStream with at least one live audio track and one live video track.
    - Peer connection, ICE connection, and signaling state reach stable/connected equivalents and no connection error dialog appears.
    - Console evidence includes client diagnostic markers and excludes secret-bearing fields.
  </behavior>
  <action>Update the two-participant Playwright smoke from Plan 01 to assert the final corrected behavior after Plans 02-04. Use existing `data-testid="participant-video"`, `data-participant-local`, MediaStream track inspection, video readiness, and peer connection state hooks if present. Keep assertions limited to two participants for Phase 1. Add failure output that names which requirement failed: MEDIA-01 first participant waiting, MEDIA-02 second join negotiation, MEDIA-03 video, MEDIA-04 audio, MEDIA-06 owner mapping, or D-05 visible terminal error.</action>
  <verify>
    <automated>cd openmeet-client && yarn test:e2e --project=chromium --grep "two participants"</automated>
  </verify>
  <acceptance_criteria>
    - Test source: asserts remote audio and remote video tracks for both participants.
    - Test source: does not make third/fourth participant stability a blocking pass condition per D-11.
    - Test output: failing assertions identify the media layer or requirement rather than only timing out.
  </acceptance_criteria>
  <done>The browser smoke is strong enough to serve as VER-01/VER-02 regression coverage for the two-participant baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Run full targeted quality gates and VPS smoke</name>
  <files>openmeet-client/e2e/multi-participant-media.spec.ts</files>
  <read_first>
    @.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
    @.planning/codebase/TESTING.md
    @openmeet-client/package.json
    @openmeet-server/Cargo.toml
    @openmeet-client/e2e/multi-participant-media.spec.ts
  </read_first>
  <action>Run the relevant client and server gates after all fixes: targeted Playwright smoke, WebRTC machine unit tests, client type-check, server cargo check, and server cargo test. Then run the two-browser smoke against `https://openmeets.eu/room/1/` where feasible per D-09, using read-only VPS logs from `ssh debian@162.19.154.153` for correlation per D-02/D-02a. If a full deployed smoke is blocked by credentials, browser permissions, deployment state, or network constraints, document the exact blocker and the strongest equivalent evidence without marking D-09 complete. Optionally observe a third participant only after the two-person pass and record it as non-blocking Phase 2 signal per D-11.</action>
  <verify>
    <automated>cd openmeet-client && yarn test:unit --run src/xstate/machines/webrtc/__tests__/webrtc.machine.test.ts && yarn type-check && yarn test:e2e --project=chromium --grep "two participants"</automated>
    <automated>cd openmeet-server && cargo check && cargo test</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: summary includes pass/fail for targeted Playwright, WebRTC machine tests, `yarn type-check`, `cargo check`, and `cargo test`.
    - VPS evidence: summary states whether `https://openmeets.eu/room/1/` two-browser verification passed, failed, or was blocked, with server/client log markers.
    - Evidence: summary includes UI/dialog state, participant count, remote track counts, connection/ICE states, and no secrets.
  </acceptance_criteria>
  <done>Automated and deployed/equivalent evidence proves or clearly blocks Phase 1 completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify deployed two-participant media</name>
  <files>openmeet-client/e2e/multi-participant-media.spec.ts</files>
  <read_first>
    @.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
    @.planning/phases/01-two-participant-media-baseline/01-UI-SPEC.md
    @openmeet-client/e2e/multi-participant-media.spec.ts
  </read_first>
  <what-built>Plans 01-04 reproduced, instrumented, and fixed the two-participant flow; this checkpoint confirms the real deployed UI/media behavior required by D-09.</what-built>
  <action>Pause after automated verification and ask the human to confirm deployed two-browser behavior on `https://openmeets.eu/room/1/`. Do not ask the human to run commands the agent can run. Use the human result only to confirm real audio/video perception and UI state that browser automation may not fully prove.</action>
  <how-to-verify>1. Open two fresh browser profiles or devices. 2. Visit `https://openmeets.eu/room/1/` in both. 3. Join as two different names with camera and microphone enabled. 4. Confirm participant one remains connected while waiting. 5. Confirm after participant two joins, both participants see the other participant's video tile and hear remote audio. 6. Confirm no blocking connection error dialog appears during the previously observed failure window. 7. If a terminal failure occurs, confirm the dialog says `Your connection to the meeting was lost. Reload the page to reconnect, or leave the meeting and join again.` and does not expose internals.</how-to-verify>
  <verify>
    <automated>cd openmeet-client && yarn test:e2e --project=chromium --grep "two participants"</automated>
    <human-check>Human reports pass/fail for deployed two-browser audio/video on `https://openmeets.eu/room/1/`.</human-check>
  </verify>
  <acceptance_criteria>
    - Browser evidence: both human-observed participants receive remote audio/video and remain connected through the failure window per D-09.
    - UI evidence: terminal disconnects, if triggered, use the existing connection error dialog with UI-SPEC copy per D-05/D-07.
    - Scope: third participant observation is optional and non-blocking per D-11.
  </acceptance_criteria>
  <done>Human verification confirms both deployed participants receive remote audio/video, or the reported failure details are captured for the next gap-closure plan.</done>
  <resume-signal>Type "approved" with observed browser/audio/video result, or describe the failure and which participant/browser lost media.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test automation → deployed site | Browser tests exercise public production-like deployment. |
| Human browser verification → phase completion | Manual media observations become release evidence. |
| Agent → logs/summary | Diagnostic evidence is written to planning summaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | Final evidence summary | mitigate | Include state names, IDs, track counts, and pass/fail only; exclude SDP, TURN credentials, tokens, cookies, env values, SSH details, and raw logs containing secrets. |
| T-01-05-02 | Denial of Service | VPS smoke | mitigate | Use two participants only for blocking verification; optional third participant is observation-only and not a load test. |
| T-01-05-03 | Spoofing | Public room verification | accept | Existing public room behavior is unchanged; Phase 1 does not expand room join exposure. |
| T-01-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run targeted client/server gates, run the two-participant browser smoke, and complete a blocking deployed human verification checkpoint.
</verification>

<success_criteria>
All Phase 1 requirements DIAG-01 through DIAG-03, MEDIA-01 through MEDIA-06, VER-01, and VER-02 are evidenced. D-12 is satisfied by the plan summary containing UI verification evidence, client/server log markers, automated/manual checks run, and any non-blocking third-participant observation.
</success_criteria>

<output>
Create `.planning/phases/01-two-participant-media-baseline/01-05-SUMMARY.md` when done. Include the root cause, fix summary, UI verification evidence, relevant client/server log markers, exact automated/manual checks run, deployed smoke result, and any non-blocking third-participant observation per D-12.
</output>
