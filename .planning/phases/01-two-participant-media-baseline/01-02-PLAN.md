---
phase: 01-two-participant-media-baseline
plan: 2
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - openmeet-client/src/services/signaling.ts
  - openmeet-client/src/xstate/machines/webrtc/types.ts
  - openmeet-client/src/xstate/machines/webrtc/actors.ts
  - openmeet-client/src/xstate/machines/webrtc/index.ts
  - openmeet-client/src/pages/MeetingPage.vue
  - openmeet-client/src/components/meeting-page/ConnectionErrorDialog.vue
autonomous: true
requirements:
  - DIAG-02
  - MEDIA-01
  - MEDIA-02
must_haves:
  truths:
    - "D-05/D-06: Peer connection failed and unexpected WebSocket close or reconnect exhaustion become blocking user-visible terminal failures."
    - "D-07: The visible error copy is recovery-focused and contains no internals or secrets."
    - "D-08: Client diagnostics include bounded WebSocket close, peer, ICE, track, stream, and participant identifiers."
  artifacts:
    - path: openmeet-client/src/services/signaling.ts
      provides: "Typed terminal disconnect/reconnect exhaustion notification"
      contains: "onTerminalDisconnect"
    - path: openmeet-client/src/components/meeting-page/ConnectionErrorDialog.vue
      provides: "Existing blocking error dialog with Phase 1 copy"
      contains: "Your connection to the meeting was lost"
  key_links:
    - from: openmeet-client/src/services/signaling.ts
      to: openmeet-client/src/xstate/machines/webrtc/actors.ts
      via: "registered terminal disconnect callback"
      pattern: "terminal|disconnect|SERVER_ERROR"
    - from: openmeet-client/src/xstate/machines/webrtc/index.ts
      to: openmeet-client/src/pages/MeetingPage.vue
      via: "machine error state and webrtcError"
      pattern: "ConnectionErrorDialog"
---

<objective>
Make terminal meeting disconnects visible to users while preserving transient recovery behavior, per D-05 through D-08.

Purpose: the reported VPS failure currently disconnects participants without visible UI feedback; Phase 1 must expose terminal failures through the existing meeting error flow.
Output: client signaling and XState changes that route WebSocket reconnect exhaustion and peer connection failure into `ConnectionErrorDialog` with safe copy.
</objective>

<execution_context>
@/Users/nikitalutsai/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nikitalutsai/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
@.planning/phases/01-two-participant-media-baseline/01-UI-SPEC.md
@.planning/phases/01-two-participant-media-baseline/01-PATTERNS.md
@openmeet-client/src/services/signaling.ts
@openmeet-client/src/xstate/machines/webrtc/types.ts
@openmeet-client/src/xstate/machines/webrtc/actors.ts
@openmeet-client/src/xstate/machines/webrtc/index.ts
@openmeet-client/src/pages/MeetingPage.vue
@openmeet-client/src/components/meeting-page/ConnectionErrorDialog.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed terminal signaling failure path</name>
  <files>openmeet-client/src/services/signaling.ts, openmeet-client/src/xstate/machines/webrtc/types.ts, openmeet-client/src/xstate/machines/webrtc/actors.ts</files>
  <read_first>
    @.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
    @.planning/phases/01-two-participant-media-baseline/01-PATTERNS.md
    @openmeet-client/src/services/signaling.ts
    @openmeet-client/src/xstate/machines/webrtc/types.ts
    @openmeet-client/src/xstate/machines/webrtc/actors.ts
  </read_first>
  <behavior>
    - Unexpected WebSocket close records close code/reason/wasClean/reconnect attempt count in bounded diagnostics.
    - Reconnect exhaustion sends a typed machine event that is handled as a terminal meeting error.
    - Intentional leave/disconnect does not send a terminal error.
  </behavior>
  <action>Add an explicit terminal disconnect notification in `SignalingService` per D-05/D-06. Use a named callback registration method or existing handler style that reports unexpected close and max reconnect exhaustion with bounded fields: code, wasClean, reconnectAttempts, maxReconnectAttempts, and a safe reason category. In `joinRoomActor`, register a named handler that sends `SERVER_ERROR` or a new explicit `SIGNALING_TERMINAL_FAILURE` event to the machine, and unregister it with `signalingService.off(...)` or equivalent cleanup when the callback actor stops. Keep transient reconnect attempts as diagnostics only; do not immediately fail the machine for browser-recoverable `disconnected` states. Do not add a Rust signaling message unless the client/server contract truly needs it; if one is introduced, update both `openmeet-server/src/signaling/message.rs` and `openmeet-client/src/services/signaling.ts` in the same task.</action>
  <verify>
    <automated>cd openmeet-client && yarn test:unit --run src/xstate/machines/webrtc/__tests__/webrtc.machine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `SignalingService` has a terminal-disconnect callback or event path and logs with `[SignalingService]` without secrets per D-08.
    - Source: `joinRoomActor` cleanup unregisters every new callback/handler it adds.
    - Behavior: intentional `disconnect()` does not route to terminal UI failure; reconnect exhaustion does.
  </acceptance_criteria>
  <done>Unexpected WebSocket close/reconnect exhaustion can reach the WebRTC machine as a typed terminal failure while normal leave remains quiet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route terminal failures into the existing error dialog</name>
  <files>openmeet-client/src/xstate/machines/webrtc/index.ts, openmeet-client/src/pages/MeetingPage.vue, openmeet-client/src/components/meeting-page/ConnectionErrorDialog.vue</files>
  <read_first>
    @.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
    @.planning/phases/01-two-participant-media-baseline/01-UI-SPEC.md
    @.planning/phases/01-two-participant-media-baseline/01-PATTERNS.md
    @openmeet-client/src/xstate/machines/webrtc/index.ts
    @openmeet-client/src/pages/MeetingPage.vue
    @openmeet-client/src/components/meeting-page/ConnectionErrorDialog.vue
  </read_first>
  <behavior>
    - `CONNECTION_STATE_CHANGED` with `failed` enters the machine error state and triggers the existing `ConnectionErrorDialog`.
    - WebSocket reconnect exhaustion enters the same visible terminal error flow.
    - Error copy exactly follows UI-SPEC and excludes SDP, TURN credentials, env values, tokens, stack traces, and raw payloads.
  </behavior>
  <action>Update the WebRTC machine and MeetingPage watchers so terminal peer connection failure and terminal signaling failure both open `ConnectionErrorDialog` per D-05/D-06. Keep transient `disconnected` states in diagnostics/status only. Update `ConnectionErrorDialog` copy to the UI-SPEC error text: `Your connection to the meeting was lost. Reload the page to reconnect, or leave the meeting and join again.` Preserve existing destructive warning styling and the `Reload Page` and `Leave Meeting` actions. Do not add a toast, new modal, or new visual system.</action>
  <verify>
    <automated>cd openmeet-client && yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - Source: `MeetingPage.vue` opens the existing dialog for machine error state and/or failed connection state, not only direct `connectionState === 'failed'` if terminal signaling failure has no peer state.
    - UI: `ConnectionErrorDialog.vue` contains the required UI-SPEC copy and no internal diagnostic strings.
    - Test command: `cd openmeet-client && yarn type-check` passes.
  </acceptance_criteria>
  <done>Terminal connection loss is visible and recoverable through the existing dialog without leaking sensitive details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket → SignalingService | Network close/error data enters browser state. |
| WebRTC/browser state → UI | Low-level connection failures become user-visible copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | ConnectionErrorDialog | mitigate | Use only UI-SPEC recovery copy; never render SDP, TURN credentials, env values, tokens, stack traces, or raw WebSocket payloads per D-07. |
| T-01-02-02 | Denial of Service | WebSocket reconnect handling | mitigate | Treat only reconnect exhaustion/unexpected terminal close as blocking; keep transient disconnected states diagnostic per D-06. |
| T-01-02-03 | Tampering | Signaling contract | mitigate | Avoid new server message; if contract changes, update Rust and TypeScript together and add serde/type tests. |
| T-01-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run client unit/type checks and manually or via Playwright force WebSocket close/reconnect exhaustion to verify the existing dialog opens with safe copy.
</verification>

<success_criteria>
D-05, D-06, D-07, and D-08 are implemented: silent terminal disconnects are visible, transient disconnects are not over-blocking, and diagnostics remain bounded and secret-free.
</success_criteria>

<output>
Create `.planning/phases/01-two-participant-media-baseline/01-02-SUMMARY.md` when done. Include the terminal failure event path, UI evidence, checks run, and any remaining disconnect reproduction notes.
</output>
