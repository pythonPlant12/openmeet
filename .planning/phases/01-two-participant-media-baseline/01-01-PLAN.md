---
phase: 01-two-participant-media-baseline
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - openmeet-client/e2e/multi-participant-media.spec.ts
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
  - VER-01
  - VER-02
must_haves:
  truths:
    - "D-01/D-03: Current two-participant behavior is reproduced against https://openmeets.eu/room/1/ or explicitly documented as blocked by access/tooling."
    - "D-04/D-09: Two fresh browser participants with audio/video enabled are exercised in the same room."
    - "DIAG-01: Evidence classifies the failure layer as signaling, SDP, ICE, stream ownership, RTP forwarding, RTCP feedback, or rendering."
  artifacts:
    - path: openmeet-client/e2e/multi-participant-media.spec.ts
      provides: "Two-participant browser smoke path with fake media and remote audio/video assertions"
      contains: "two participants"
  key_links:
    - from: openmeet-client/e2e/multi-participant-media.spec.ts
      to: https://openmeets.eu/room/1/
      via: "PLAYWRIGHT_BASE_URL or explicit navigation target"
      pattern: "openmeets\.eu|baseURL"
---

<objective>
Create the Phase 1 diagnostic baseline before applying fixes, per D-01 through D-04 and D-09/D-10. The executor must first attempt the deployed VPS two-participant failure using read-only logs and browser evidence, then make the existing Playwright media smoke narrow enough to prove the same two-person path locally or against a supplied base URL. Use `ssh debian@162.19.154.153` for VPS diagnostics when needed; never persist or display the password.

Purpose: prevent guessing at the root cause and create repeatable evidence for DIAG-01, DIAG-02, DIAG-03, VER-01, and VER-02.
Output: a runnable two-participant smoke check plus a summary that names the observed failing layer and cites client/server log markers.
</objective>

<execution_context>
@/Users/nikitalutsai/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nikitalutsai/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
@.planning/phases/01-two-participant-media-baseline/01-RESEARCH.md
@.planning/phases/01-two-participant-media-baseline/01-PATTERNS.md
@.planning/codebase/TESTING.md
@openmeet-client/e2e/multi-participant-media.spec.ts
@openmeet-client/playwright.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce and classify the deployed two-participant failure</name>
  <files>openmeet-client/e2e/multi-participant-media.spec.ts</files>
  <read_first>
    @.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
    @.planning/phases/01-two-participant-media-baseline/01-RESEARCH.md
    @.planning/phases/01-two-participant-media-baseline/01-PATTERNS.md
    @openmeet-client/e2e/multi-participant-media.spec.ts
    @openmeet-client/playwright.config.ts
  </read_first>
  <behavior>
    - The smoke opens two isolated browser contexts for one room and grants camera/microphone permissions.
    - The smoke records browser console events containing [SignalingService], [WebRTCServiceSFU], [webrtcMachine], Connection/ICE states, remote track IDs, stream IDs, and connection-error dialog visibility.
    - The smoke can target https://openmeets.eu/room/1/ for D-01/D-03 without embedding SSH credentials or secret values.
  </behavior>
  <action>Update the existing Playwright media smoke so there is an explicit two-participant baseline test for D-01/D-04/D-09. Keep the existing fake media launch flags and isolated context pattern. Add bounded console/log collection for client diagnostics only; do not capture SDP bodies, tokens, TURN credentials, environment values, cookies, or full WebSocket payloads. The test should assert both participants join the same room, local video attaches for each participant, remote video elements appear for the other participant, remote streams contain at least one live video track and one live audio track, and no connection error dialog is shown during the observation window. Allow the base URL/room URL to be supplied by Playwright configuration or environment so the executor can run against localhost and https://openmeets.eu/room/1/ per D-03.</action>
  <verify>
    <automated>cd openmeet-client && yarn test:e2e --project=chromium --grep "two participants"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `openmeet-client/e2e/multi-participant-media.spec.ts` contains one focused two-participant test whose title includes "two participants" and does not make 3+ participant stability a blocking assertion per D-11.
    - Browser evidence: test output or trace shows two contexts joined one room with audio/video enabled and records whether each side receives remote audio/video.
    - Security: captured diagnostics exclude SDP text, TURN credentials, tokens, cookies, env values, SSH credentials, and stack traces per D-02/D-07/D-08.
  </acceptance_criteria>
  <done>The executor can run one command to classify the deployed or local two-participant failure layer and has browser-side evidence for DIAG-01/DIAG-02/VER-02.</done>
</task>

<task type="auto">
  <name>Task 2: Inspect VPS logs read-only and correlate server markers</name>
  <files>openmeet-client/e2e/multi-participant-media.spec.ts</files>
  <read_first>
    @.planning/phases/01-two-participant-media-baseline/01-CONTEXT.md
    @.planning/phases/01-two-participant-media-baseline/01-RESEARCH.md
    @.planning/codebase/CONCERNS.md
    @openmeet-server/src/signaling/handler.rs
    @openmeet-server/src/sfu/room.rs
  </read_first>
  <action>After the browser reproduction attempt, inspect production/VPS server and container logs in read-only mode per D-02/D-02a. Use `ssh debian@162.19.154.153` and already configured Docker access if available; if access is unavailable, record the blocker in the plan summary without asking the user to paste credentials again. Correlate timestamps with the Playwright/manual browser evidence and classify server progress through room join, offer, answer, ICE candidate handling, on_track registration, StreamOwner send, RTP packet forwarding, RTCP feedback, WebSocket close, and connection failure. Do not edit VPS files and do not print secrets.</action>
  <verify>
    <automated>cd openmeet-client && yarn test:e2e --project=chromium --grep "two participants"</automated>
  </verify>
  <acceptance_criteria>
    - CLI evidence: summary includes exact commands run for read-only log inspection, redacted where necessary, and reports whether `ssh debian@162.19.154.153` access succeeded or was blocked.
    - Server evidence: summary names which of DIAG-03 checkpoints were observed: membership, offer/answer, incoming track registration, forwarding target selection, RTP forwarding, RTCP feedback.
    - Scope: no VPS-side file edits, deployments, env reads, or credential recording occurred per D-02.
  </acceptance_criteria>
  <done>The failing layer is identified or the evidence gap is explicit, bounded, and ready for the targeted fix wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser automation → production UI | Test browser connects to public OpenMeet pages and untrusted browser console output is collected. |
| Agent → VPS logs | Read-only operational diagnostics may include sensitive runtime details if mishandled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | Playwright console/log capture | mitigate | Capture bounded state, track IDs, stream IDs, and visible UI state only; exclude SDP, TURN credentials, cookies, tokens, env values, and SSH details per D-02/D-07/D-08. |
| T-01-01-02 | Tampering | VPS diagnostics | mitigate | Use read-only log inspection only; do not edit VPS files or deployment config in this plan. |
| T-01-01-03 | Denial of Service | Browser smoke against production | mitigate | Run two fresh participants only for Phase 1; do not load-test or make 3+ stability blocking per D-04/D-11. |
| T-01-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; if executor proposes an install, stop and require package legitimacy audit. |
</threat_model>

<verification>
Run the two-participant Playwright smoke locally and, where feasible, against `https://openmeets.eu/room/1/`. The plan summary must include browser evidence, server log markers, and the classified failing layer.
</verification>

<success_criteria>
DIAG-01/DIAG-02/DIAG-03 are satisfied for the current failure: the executor can say where the two-participant path breaks and has repeatable browser evidence to reproduce or simulate it. VER-01/VER-02 have an executable smoke path that later plans can keep green.
</success_criteria>

<output>
Create `.planning/phases/01-two-participant-media-baseline/01-01-SUMMARY.md` when done. Include D-01 through D-04 and D-09/D-10 evidence, relevant client/server log markers, exact checks run, and whether a non-blocking third participant was intentionally skipped per D-11.
</output>
